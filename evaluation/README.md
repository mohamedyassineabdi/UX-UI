# Reference audit evaluation

This directory intentionally contains no client or expert data. Future approved reference audits should record fixture/site ID, rule ID, page ID, ground-truth outcome, severity, reviewer agreement, notes, and (for AI) provider/model/prompt version. Compare categorical machine findings for precision, recall, F1, false-positive rate, false-negative rate, and coverage. This harness does not calibrate Phase 3A weights.
