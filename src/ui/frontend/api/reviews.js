export const getReview = (api, id) => api.request(`/api/audits/${encodeURIComponent(id)}/review`).then((response) => api.json(response, "Unable to load review state."));
export const saveRevision = (api, id, changes, expectedRevisionId) => api.request(`/api/audits/${encodeURIComponent(id)}/revisions`, { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ findingChanges: changes, expectedRevisionId }) }).then((response) => api.json(response, "Unable to save review changes."));
export const transitionRevision = (api, id, action, revisionId) => api.request(`/api/audits/${encodeURIComponent(id)}/${action}`, { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ revisionId }) }).then((response) => api.json(response, `Unable to ${action} revision.`));
