window.__UX_UI_AUDITOR_CONFIG__ = {
  // The authenticated launcher is served by the API. Do not accept query/localStorage
  // overrides because bearer headers must never be redirected by untrusted input.
  apiBaseUrl: "",
};
